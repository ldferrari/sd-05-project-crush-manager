const login = require('./login');
const pegouCrush = require('./pegouCrush');
const token = require('./token');
const createCrush = require('./createCrush');

module.exports = { login, pegouCrush, token, createCrush };
