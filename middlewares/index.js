const login = require('./login');
const auth = require('./auth');
const checkCrush = require('./checkCrush');
const newCrush = require('./newCrush');
const getById = require('./getById');
const updateCrush = require('./updateCrush');
const excludeCrush = require('./excludeCrush');

module.exports = {
  login,
  auth,
  checkCrush,
  newCrush,
  getById,
  updateCrush,
  excludeCrush,
};
